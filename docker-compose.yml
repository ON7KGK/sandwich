version: '3.8'

services:
  sandwich-app:
    build: .
    container_name: sandwich-tentation
    ports:
      - "8080:8080"
    volumes:
      # Utiliser un volume Docker nommé pour la persistence
      - sandwich-data:/app
    restart: unless-stopped
    environment:
      - FLASK_ENV=production

volumes:
  sandwich-data:
